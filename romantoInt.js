var romanToInt = function(s) {
    const array =  ['I', 'V', 'X', 'L', 'C', 'D', 'M'];
    const map = {
        I : 1,
        V : 5,
        X : 10,
        L : 50,
        C : 100,
        D : 500,
        M : 1000
    }

    const convertMap = function(){
        if(map.i > map(i-1)){
            return map.i - map(i-1);
        }else{
            return map.i + map(i-1);
        }

    }

};